server:
  port: 8081

jwt:
  secret: ${JWT_SECRET:dGVzdC1vbmx5LXNlY3JldC1ub3QtZm9yLXByb2R1Y3Rpb24tMTIz}

spring:
  application:
    name: auth

  docker:
    compose:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: create-drop

logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE

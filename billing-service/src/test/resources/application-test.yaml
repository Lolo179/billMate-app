spring:
  docker:
    compose:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: create

  flyway:
    enabled: false

  sql:
    init:
      mode: never

billmate:
  kafka:
    enabled: false

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
spring:
  docker:
    compose:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: create

  flyway:
    enabled: false

  sql:
    init:
      mode: never

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
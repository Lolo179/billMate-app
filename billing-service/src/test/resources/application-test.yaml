spring:
  docker:
    compose:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: create

  flyway:
    enabled: false

  sql:
    init:
      mode: never

logging:
  level:

    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace